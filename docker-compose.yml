version: '3.7'

services:
  app:
    image: selfnurse:0.0.1
    restart: unless-stopped
    ports:
      - 3000:3000
    environment:
      - JWT_SECRET_KEY=Ct0hqIlulr~~ecVa-z=y#Eu6.6]i]yyohPSLp!wQ2SxW'6.]_H
      - NODE_ENV=Development
      - PORT=3000
      - DB_HOST=mysqldb
      - DB_PORT=3306
      - DB_NAME=self_nurse_db
      - DB_LOGGING=false
      - MYSQL_USER=root
      - MYSQL_PASSWORD=toor
  mysqldb:
    image: mysql:8.0.33-debian
    restart: unless-stopped
    ports:
      - 3037:3036
    volumes:
      - D:\tmp\selfnurse-data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=toor
      - MYSQL_DATABASE=self_nurse_db
